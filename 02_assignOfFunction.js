console.log(`-------------------------Length & Sqare of word------------------------------------------`);
function squareOfWordLength(str){
    var value=str.length
console.log(`1.1 length of word ${value}`);

  console.log(`2.1 square ${value*value}`);
  return square
}
 squareOfWordLength("JavaScript");
squareOfWordLength("Google Chrome");
squareOfWordLength("Developer Smart");
var square
console.log(`--------------------------Division & Multiplication------------------------------------`);
var string=function(){
  var position="I am Angular Developer"
  console.log(`given string is: ${position}`);
  console.log(`length of string: ${position.length}`);
  var totalWord=position .split(" ");
  console.log(`Total word in given string ${totalWord.length}`);
  console.log(`2.1.Division Result: ${(position.length)/totalWord.length}`);
  console.log(`2.2.Multiplication  Result: ${(position.length)*totalWord.length}`);
  
}
string();